# Deprecated Agent Format

This file is kept only as a migration note.

Based on `raw/codex/suagents.md`, Codex custom agents are defined as standalone TOML files under `.codex/agents/`.

Use [`docs-ingest.toml`](./docs-ingest.toml) as the active project-scoped agent definition for docs ingest work.
